# Repository profile suggestions for maintainers.
#
# GitHub topics are managed in the repository settings or through the GitHub API.
# Keep this file in sync with the public README, release notes, and project scope.
name: ai-model-gateway
description: Self-hosted LLM operations gateway with routing, fallback, telemetry, benchmarks, diagnostics, and rollback.
homepage: ""
topics:
  - ai-gateway
  - llm
  - llmops
  - self-hosted
  - openai-compatible
  - model-routing
  - fallback
  - benchmark
  - reverse-proxy
  - telemetry
  - golang
  - preact
  - observability
visibility: public
primary_language: Go
areas:
  routing:
    - internal/router
    - internal/proxy
    - configs
  admin-ui:
    - web/admin
  monitoring-pricing:
    - internal/telemetry
    - internal/infra
  packaging:
    - cmd/aigw
    - internal/release
    - .github/workflows
