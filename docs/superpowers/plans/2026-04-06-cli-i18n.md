# CLI i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement CLI output internationalization supporting 7 languages (zh/en/ja/ko/es/fr/de) via `-lang` flag.

**Architecture:** Create `internal/cli/i18n.go` with simple key-value lookup. Store translations in JSON files. Modify `cmd/gateway/main.go` to use i18n for all output.

**Tech Stack:** Go, embed, JSON

---

## File Structure

| File | Purpose |
|------|---------|
| `internal/cli/i18n.go` | Simple i18n implementation for CLI |
| `internal/cli/locales/en.json` | English translations |
| `internal/cli/locales/zh.json` | Chinese translations |
| `internal/cli/locales/ja.json` | Japanese translations |
| `internal/cli/locales/ko.json` | Korean translations |
| `internal/cli/locales/es.json` | Spanish translations |
| `internal/cli/locales/fr.json` | French translations |
| `internal/cli/locales/de.json` | German translations |
| `internal/cli/i18n_test.go` | Unit tests |
| `cmd/gateway/main.go` | Modified to use i18n |

---

## Task 1: Create CLI i18n Package

**Files:**
- Create: `internal/cli/i18n.go`

---

- [ ] **Step 1.1: Create i18n.go**

Create `internal/cli/i18n.go`:

```go
package cli

import (
	"embed"
	"encoding/json"
	"fmt"
	"os"
	"strings"
)

//go:embed locales/*.json
var localeFS embed.FS

var (
	currentLang = "zh"
	catalog     map[string]string
)

// SetLanguage sets the current language
func SetLanguage(lang string) {
	currentLang = normalizeLang(lang)
	loadCatalog()
}

// T translates a key with optional arguments
func T(key string, args ...interface{}) string {
	if catalog == nil {
		loadCatalog()
	}
	msg, ok := catalog[key]
	if !ok {
		return key
	}
	if len(args) > 0 {
		return fmt.Sprintf(msg, args...)
	}
	return msg
}

func loadCatalog() {
	catalog = make(map[string]string)
	data, err := localeFS.ReadFile(fmt.Sprintf("locales/%s.json", currentLang))
	if err != nil {
		// Fall back to zh
		data, _ = localeFS.ReadFile("locales/zh.json")
	}

	var nested map[string]map[string]string
	if err := json.Unmarshal(data, &nested); err != nil {
		return
	}

	// Flatten nested structure
	for category, messages := range nested {
		for key, value := range messages {
			catalog[category+"."+key] = value
		}
	}
}

func normalizeLang(lang string) string {
	switch strings.ToLower(lang) {
	case "zh", "zh-cn", "zh-tw":
		return "zh"
	case "en", "en-us", "en-gb":
		return "en"
	case "ja":
		return "ja"
	case "ko":
		return "ko"
	case "es":
		return "es"
	case "fr":
		return "fr"
	case "de":
		return "de"
	default:
		return "zh"
	}
}

// GetLanguageFromEnv gets language from LANG environment variable
func GetLanguageFromEnv() string {
	lang := os.Getenv("LANG")
	if lang == "" {
		return "zh"
	}
	// LANG is usually like en_US.UTF-8
	parts := strings.Split(lang, "_")
	if len(parts) > 0 {
		return strings.ToLower(parts[0])
	}
	return "zh"
}
```

---

- [ ] **Step 1.2: Create locales directory with zh.json**

Create `internal/cli/locales/zh.json`:

```json
{
  "cli": {
    "gateway_failed": "网关启动失败: %v",
    "install_failed": "安装失败: %v",
    "uninstall_failed": "卸载失败: %v",
    "start_failed": "启动失败: %v",
    "stop_failed": "停止失败: %v",
    "status_failed": "状态检查失败: %v",
    "unknown_command": "未知命令: %s",
    "config_not_found": "配置文件未找到: %s",
    "config_invalid": "配置验证失败: %v",
    "config_valid": "✓ 配置有效",
    "health_check_failed": "健康检查失败: %v",
    "service_installed": "✓ 服务安装成功",
    "service_uninstalled": "✓ 服务卸载成功",
    "service_started": "✓ 服务启动成功",
    "service_stopped": "✓ 服务停止成功",
    "service_not_installed": "服务未安装",
    "service_status": "服务状态: %s",
    "use_install_for_service": "使用 'gateway install' 安装为 Windows 服务",
    "listen": "监听",
    "providers": "提供商",
    "admin": "管理",
    "health_check": "健康检查",
    "bridge": "桥接"
  },
  "usage": {
    "title": "用法: gateway [命令] [选项]",
    "commands_title": "命令:",
    "options_title": "选项:",
    "start_desc": "启动网关服务器",
    "validate_desc": "验证配置文件",
    "health_desc": "检查网关健康状态",
    "install_desc": "安装为 Windows 服务",
    "uninstall_desc": "卸载 Windows 服务",
    "service_start_desc": "启动 Windows 服务",
    "service_stop_desc": "停止 Windows 服务",
    "service_status_desc": "检查 Windows 服务状态",
    "config_desc": "配置文件路径 (默认: configs/config.yaml)",
    "lang_desc": "界面语言 (默认: zh)"
  }
}
```

---

- [ ] **Step 1.3: Create en.json**

Create `internal/cli/locales/en.json`:

```json
{
  "cli": {
    "gateway_failed": "Gateway failed: %v",
    "install_failed": "Install failed: %v",
    "uninstall_failed": "Uninstall failed: %v",
    "start_failed": "Start failed: %v",
    "stop_failed": "Stop failed: %v",
    "status_failed": "Status check failed: %v",
    "unknown_command": "Unknown command: %s",
    "config_not_found": "Config file not found: %s",
    "config_invalid": "Config validation failed: %v",
    "config_valid": "✓ Configuration is valid",
    "health_check_failed": "Health check failed: %v",
    "service_installed": "✓ Service installed successfully",
    "service_uninstalled": "✓ Service uninstalled successfully",
    "service_started": "✓ Service started successfully",
    "service_stopped": "✓ Service stopped successfully",
    "service_not_installed": "Service not installed",
    "service_status": "Service status: %s",
    "use_install_for_service": "Use 'gateway install' to install as Windows service",
    "listen": "Listen",
    "providers": "Providers",
    "admin": "Admin",
    "health_check": "Health check",
    "bridge": "Bridge"
  },
  "usage": {
    "title": "Usage: gateway [command] [options]",
    "commands_title": "Commands:",
    "options_title": "Options:",
    "start_desc": "Start the gateway server",
    "validate_desc": "Validate configuration file",
    "health_desc": "Check gateway health status",
    "install_desc": "Install as Windows service",
    "uninstall_desc": "Uninstall Windows service",
    "service_start_desc": "Start Windows service",
    "service_stop_desc": "Stop Windows service",
    "service_status_desc": "Check Windows service status",
    "config_desc": "Path to config file (default: configs/config.yaml)",
    "lang_desc": "Interface language (default: en)"
  }
}
```

---

- [ ] **Step 1.4: Verify package compiles**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go build ./internal/cli/...
```

Expected: No errors

---

- [ ] **Step 1.5: Commit**

```bash
git add internal/cli/i18n.go internal/cli/locales/
git commit -m "feat(cli): add i18n package with zh/en translations

- Add T() function for message translation
- Support zh/en languages
- Auto-detect from LANG environment variable"
```

---

## Task 2: Add Remaining Languages

**Files:**
- Create: `internal/cli/locales/ja.json`
- Create: `internal/cli/locales/ko.json`
- Create: `internal/cli/locales/es.json`
- Create: `internal/cli/locales/fr.json`
- Create: `internal/cli/locales/de.json`

---

- [ ] **Step 2.1: Create ja.json**

Create `internal/cli/locales/ja.json`:

```json
{
  "cli": {
    "gateway_failed": "ゲートウェイの起動に失敗しました: %v",
    "install_failed": "インストールに失敗しました: %v",
    "uninstall_failed": "アンインストールに失敗しました: %v",
    "start_failed": "開始に失敗しました: %v",
    "stop_failed": "停止に失敗しました: %v",
    "status_failed": "ステータス確認に失敗しました: %v",
    "unknown_command": "不明なコマンド: %s",
    "config_not_found": "設定ファイルが見つかりません: %s",
    "config_invalid": "設定の検証に失敗しました: %v",
    "config_valid": "✓ 設定は有効です",
    "health_check_failed": "ヘルスチェックに失敗しました: %v",
    "service_installed": "✓ サービスが正常にインストールされました",
    "service_uninstalled": "✓ サービスが正常にアンインストールされました",
    "service_started": "✓ サービスが正常に開始されました",
    "service_stopped": "✓ サービスが正常に停止されました",
    "service_not_installed": "サービスがインストールされていません",
    "service_status": "サービスステータス: %s",
    "use_install_for_service": "'gateway install' を使用してWindowsサービスとしてインストール",
    "listen": "リスン",
    "providers": "プロバイダー",
    "admin": "管理",
    "health_check": "ヘルスチェック",
    "bridge": "ブリッジ"
  },
  "usage": {
    "title": "使い方: gateway [コマンド] [オプション]",
    "commands_title": "コマンド:",
    "options_title": "オプション:",
    "start_desc": "ゲートウェイサーバーを起動",
    "validate_desc": "設定ファイルを検証",
    "health_desc": "ゲートウェイのヘルス状態を確認",
    "install_desc": "Windowsサービスとしてインストール",
    "uninstall_desc": "Windowsサービスをアンインストール",
    "service_start_desc": "Windowsサービスを開始",
    "service_stop_desc": "Windowsサービスを停止",
    "service_status_desc": "Windowsサービスの状態を確認",
    "config_desc": "設定ファイルのパス (デフォルト: configs/config.yaml)",
    "lang_desc": "インターフェース言語 (デフォルト: ja)"
  }
}
```

---

- [ ] **Step 2.2: Create ko.json**

Create `internal/cli/locales/ko.json`:

```json
{
  "cli": {
    "gateway_failed": "게이트웨이 시작 실패: %v",
    "install_failed": "설치 실패: %v",
    "uninstall_failed": "제거 실패: %v",
    "start_failed": "시작 실패: %v",
    "stop_failed": "중지 실패: %v",
    "status_failed": "상태 확인 실패: %v",
    "unknown_command": "알 수 없는 명령: %s",
    "config_not_found": "설정 파일을 찾을 수 없음: %s",
    "config_invalid": "설정 검증 실패: %v",
    "config_valid": "✓ 설정이 유효합니다",
    "health_check_failed": "상태 확인 실패: %v",
    "service_installed": "✓ 서비스가 성공적으로 설치되었습니다",
    "service_uninstalled": "✓ 서비스가 성공적으로 제거되었습니다",
    "service_started": "✓ 서비스가 성공적으로 시작되었습니다",
    "service_stopped": "✓ 서비스가 성공적으로 중지되었습니다",
    "service_not_installed": "서비스가 설치되지 않음",
    "service_status": "서비스 상태: %s",
    "use_install_for_service": "'gateway install'을 사용하여 Windows 서비스로 설치",
    "listen": "수신",
    "providers": "제공자",
    "admin": "관리",
    "health_check": "상태 확인",
    "bridge": "브리지"
  },
  "usage": {
    "title": "사용법: gateway [명령] [옵션]",
    "commands_title": "명령:",
    "options_title": "옵션:",
    "start_desc": "게이트웨이 서버 시작",
    "validate_desc": "설정 파일 검증",
    "health_desc": "게이트웨이 상태 확인",
    "install_desc": "Windows 서비스로 설치",
    "uninstall_desc": "Windows 서비스 제거",
    "service_start_desc": "Windows 서비스 시작",
    "service_stop_desc": "Windows 서비스 중지",
    "service_status_desc": "Windows 서비스 상태 확인",
    "config_desc": "설정 파일 경로 (기본값: configs/config.yaml)",
    "lang_desc": "인터페이스 언어 (기본값: ko)"
  }
}
```

---

- [ ] **Step 2.3: Create es.json, fr.json, de.json**

Create `internal/cli/locales/es.json`:

```json
{
  "cli": {
    "gateway_failed": "Fallo del gateway: %v",
    "install_failed": "Fallo de instalación: %v",
    "uninstall_failed": "Fallo de desinstalación: %v",
    "start_failed": "Fallo de inicio: %v",
    "stop_failed": "Fallo de detención: %v",
    "status_failed": "Fallo de verificación de estado: %v",
    "unknown_command": "Comando desconocido: %s",
    "config_not_found": "Archivo de configuración no encontrado: %s",
    "config_invalid": "Fallo de validación de configuración: %v",
    "config_valid": "✓ La configuración es válida",
    "health_check_failed": "Fallo de verificación de salud: %v",
    "service_installed": "✓ Servicio instalado exitosamente",
    "service_uninstalled": "✓ Servicio desinstalado exitosamente",
    "service_started": "✓ Servicio iniciado exitosamente",
    "service_stopped": "✓ Servicio detenido exitosamente",
    "service_not_installed": "Servicio no instalado",
    "service_status": "Estado del servicio: %s",
    "use_install_for_service": "Use 'gateway install' para instalar como servicio de Windows",
    "listen": "Escuchar",
    "providers": "Proveedores",
    "admin": "Admin",
    "health_check": "Verificación de salud",
    "bridge": "Puente"
  },
  "usage": {
    "title": "Uso: gateway [comando] [opciones]",
    "commands_title": "Comandos:",
    "options_title": "Opciones:",
    "start_desc": "Iniciar el servidor gateway",
    "validate_desc": "Validar archivo de configuración",
    "health_desc": "Verificar estado del gateway",
    "install_desc": "Instalar como servicio de Windows",
    "uninstall_desc": "Desinstalar servicio de Windows",
    "service_start_desc": "Iniciar servicio de Windows",
    "service_stop_desc": "Detener servicio de Windows",
    "service_status_desc": "Verificar estado del servicio de Windows",
    "config_desc": "Ruta al archivo de configuración (por defecto: configs/config.yaml)",
    "lang_desc": "Idioma de la interfaz (por defecto: es)"
  }
}
```

Create `internal/cli/locales/fr.json`:

```json
{
  "cli": {
    "gateway_failed": "Échec de la passerelle: %v",
    "install_failed": "Échec de l'installation: %v",
    "uninstall_failed": "Échec de la désinstallation: %v",
    "start_failed": "Échec du démarrage: %v",
    "stop_failed": "Échec de l'arrêt: %v",
    "status_failed": "Échec de la vérification du statut: %v",
    "unknown_command": "Commande inconnue: %s",
    "config_not_found": "Fichier de configuration non trouvé: %s",
    "config_invalid": "Échec de la validation de la configuration: %v",
    "config_valid": "✓ La configuration est valide",
    "health_check_failed": "Échec de la vérification de santé: %v",
    "service_installed": "✓ Service installé avec succès",
    "service_uninstalled": "✓ Service désinstallé avec succès",
    "service_started": "✓ Service démarré avec succès",
    "service_stopped": "✓ Service arrêté avec succès",
    "service_not_installed": "Service non installé",
    "service_status": "Statut du service: %s",
    "use_install_for_service": "Utilisez 'gateway install' pour installer comme service Windows",
    "listen": "Écoute",
    "providers": "Fournisseurs",
    "admin": "Admin",
    "health_check": "Vérification de santé",
    "bridge": "Pont"
  },
  "usage": {
    "title": "Utilisation: gateway [commande] [options]",
    "commands_title": "Commandes:",
    "options_title": "Options:",
    "start_desc": "Démarrer le serveur gateway",
    "validate_desc": "Valider le fichier de configuration",
    "health_desc": "Vérifier l'état de la passerelle",
    "install_desc": "Installer comme service Windows",
    "uninstall_desc": "Désinstaller le service Windows",
    "service_start_desc": "Démarrer le service Windows",
    "service_stop_desc": "Arrêter le service Windows",
    "service_status_desc": "Vérifier l'état du service Windows",
    "config_desc": "Chemin vers le fichier de configuration (par défaut: configs/config.yaml)",
    "lang_desc": "Langue de l'interface (par défaut: fr)"
  }
}
```

Create `internal/cli/locales/de.json`:

```json
{
  "cli": {
    "gateway_failed": "Gateway-Fehler: %v",
    "install_failed": "Installationsfehler: %v",
    "uninstall_failed": "Deinstallationsfehler: %v",
    "start_failed": "Startfehler: %v",
    "stop_failed": "Stoppfehler: %v",
    "status_failed": "Statusprüfungsfehler: %v",
    "unknown_command": "Unbekannter Befehl: %s",
    "config_not_found": "Konfigurationsdatei nicht gefunden: %s",
    "config_invalid": "Konfigurationsvalidierung fehlgeschlagen: %v",
    "config_valid": "✓ Konfiguration ist gültig",
    "health_check_failed": "Integritätsprüfung fehlgeschlagen: %v",
    "service_installed": "✓ Dienst erfolgreich installiert",
    "service_uninstalled": "✓ Dienst erfolgreich deinstalliert",
    "service_started": "✓ Dienst erfolgreich gestartet",
    "service_stopped": "✓ Dienst erfolgreich gestoppt",
    "service_not_installed": "Dienst nicht installiert",
    "service_status": "Dienststatus: %s",
    "use_install_for_service": "Verwenden Sie 'gateway install', um als Windows-Dienst zu installieren",
    "listen": "Listen",
    "providers": "Anbieter",
    "admin": "Admin",
    "health_check": "Integritätsprüfung",
    "bridge": "Brücke"
  },
  "usage": {
    "title": "Verwendung: gateway [befehl] [optionen]",
    "commands_title": "Befehle:",
    "options_title": "Optionen:",
    "start_desc": "Gateway-Server starten",
    "validate_desc": "Konfigurationsdatei validieren",
    "health_desc": "Gateway-Status prüfen",
    "install_desc": "Als Windows-Dienst installieren",
    "uninstall_desc": "Windows-Dienst deinstallieren",
    "service_start_desc": "Windows-Dienst starten",
    "service_stop_desc": "Windows-Dienst stoppen",
    "service_status_desc": "Windows-Dienststatus prüfen",
    "config_desc": "Pfad zur Konfigurationsdatei (Standard: configs/config.yaml)",
    "lang_desc": "Sprache der Benutzeroberfläche (Standard: de)"
  }
}
```

---

- [ ] **Step 2.4: Commit**

```bash
git add internal/cli/locales/ja.json internal/cli/locales/ko.json \
  internal/cli/locales/es.json internal/cli/locales/fr.json internal/cli/locales/de.json
git commit -m "feat(cli): add remaining language translations

- Add Japanese, Korean, Spanish
- Add French, German translations
- Complete 7 language support for CLI"
```

---

## Task 3: Modify cmd/gateway/main.go

**Files:**
- Modify: `cmd/gateway/main.go`

---

- [ ] **Step 3.1: Read current main.go structure**

Run:
```bash
head -80 /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway/cmd/gateway/main.go
```

---

- [ ] **Step 3.2: Add lang flag and init i18n**

In `cmd/gateway/main.go`, after imports add:

```go
import (
    // ... existing imports
    "ai-model-gateway/internal/cli"
)

var cliLang string

func init() {
    // Check for -lang flag early
    for i, arg := range os.Args {
        if arg == "-lang" && i+1 < len(os.Args) {
            cliLang = os.Args[i+1]
            break
        }
        if strings.HasPrefix(arg, "-lang=") {
            cliLang = strings.TrimPrefix(arg, "-lang=")
            break
        }
    }
    
    // If no -lang flag, check LANG env
    if cliLang == "" {
        cliLang = cli.GetLanguageFromEnv()
    }
    
    cli.SetLanguage(cliLang)
}
```

---

- [ ] **Step 3.3: Update error messages to use cli.T()**

Replace all error output lines:

```go
// Change from:
fmt.Fprintf(os.Stderr, "gateway failed: %v\n", err)

// To:
fmt.Fprintf(os.Stderr, cli.T("cli.gateway_failed")+"\n", err)
```

Do this for all messages:
- "gateway failed" → `cli.T("cli.gateway_failed")`
- "Install failed" → `cli.T("cli.install_failed")`
- "Uninstall failed" → `cli.T("cli.uninstall_failed")`
- etc.

---

- [ ] **Step 3.4: Update printUsage function**

Replace the printUsage function:

```go
func printUsage() {
    fmt.Println(cli.T("usage.title"))
    fmt.Println("")
    fmt.Println(cli.T("usage.commands_title"))
    fmt.Println("  start           " + cli.T("usage.start_desc"))
    fmt.Println("  validate        " + cli.T("usage.validate_desc"))
    fmt.Println("  health          " + cli.T("usage.health_desc"))
    fmt.Println("  install         " + cli.T("usage.install_desc"))
    fmt.Println("  uninstall       " + cli.T("usage.uninstall_desc"))
    fmt.Println("  service-start   " + cli.T("usage.service_start_desc"))
    fmt.Println("  service-stop    " + cli.T("usage.service_stop_desc"))
    fmt.Println("  service-status  " + cli.T("usage.service_status_desc"))
    fmt.Println("")
    fmt.Println(cli.T("usage.options_title"))
    fmt.Println("  -config path    " + cli.T("usage.config_desc"))
    fmt.Println("  -lang lang      " + cli.T("usage.lang_desc"))
}
```

---

- [ ] **Step 3.5: Update cmdValidate output**

```go
// Change:
fmt.Println("✓ Configuration is valid")
fmt.Printf("  Listen: %s\n", cfg.Server.Listen)

// To:
fmt.Println(cli.T("cli.config_valid"))
fmt.Printf("  "+cli.T("cli.listen")+": %s\n", cfg.Server.Listen)
fmt.Printf("  "+cli.T("cli.providers")+": %d\n", len(cfg.Providers))
```

---

- [ ] **Step 3.6: Test CLI with different languages**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go build -o gateway-test.exe ./cmd/gateway

# Test Chinese
.\gateway-test.exe -lang zh validate

# Test English
.\gateway-test.exe -lang en validate
```

Expected: Output language changes based on -lang flag

---

- [ ] **Step 3.7: Commit**

```bash
git add cmd/gateway/main.go
git commit -m "feat(cli): integrate i18n into main.go

- Add -lang flag support
- Auto-detect from LANG environment variable
- Translate all error messages and usage info
- Support 7 languages"
```

---

## Task 4: Add Unit Tests

**Files:**
- Create: `internal/cli/i18n_test.go`

---

- [ ] **Step 4.1: Create tests**

Create `internal/cli/i18n_test.go`:

```go
package cli

import (
	"testing"
)

func TestSetLanguage(t *testing.T) {
	SetLanguage("en")
	if currentLang != "en" {
		t.Errorf("expected lang=en, got %s", currentLang)
	}

	SetLanguage("zh")
	if currentLang != "zh" {
		t.Errorf("expected lang=zh, got %s", currentLang)
	}
}

func TestT(t *testing.T) {
	SetLanguage("en")
	
	// Test existing key
	msg := T("cli.config_valid")
	if msg != "✓ Configuration is valid" {
		t.Errorf("unexpected message: %s", msg)
	}
	
	// Test missing key (returns key)
	msg = T("nonexistent.key")
	if msg != "nonexistent.key" {
		t.Errorf("expected key for missing, got: %s", msg)
	}
}

func TestT_Formatted(t *testing.T) {
	SetLanguage("en")
	
	msg := T("cli.gateway_failed", "test error")
	expected := "Gateway failed: test error"
	if msg != expected {
		t.Errorf("expected %q, got %q", expected, msg)
	}
}

func TestNormalizeLang(t *testing.T) {
	tests := []struct {
		input    string
		expected string
	}{
		{"zh", "zh"},
		{"zh-CN", "zh"},
		{"en", "en"},
		{"en-US", "en"},
		{"ja", "ja"},
		{"unknown", "zh"},
	}
	
	for _, tt := range tests {
		result := normalizeLang(tt.input)
		if result != tt.expected {
			t.Errorf("normalizeLang(%q) = %q, want %q", tt.input, result, tt.expected)
		}
	}
}

func TestLanguageSwitching(t *testing.T) {
	// Test Chinese
	SetLanguage("zh")
	msg := T("cli.config_valid")
	if msg != "✓ 配置有效" {
		t.Errorf("Chinese translation failed: %s", msg)
	}
	
	// Test English
	SetLanguage("en")
	msg = T("cli.config_valid")
	if msg != "✓ Configuration is valid" {
		t.Errorf("English translation failed: %s", msg)
	}
}
```

---

- [ ] **Step 4.2: Run tests**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/cli/... -v
```

Expected: All tests pass

---

- [ ] **Step 4.3: Commit**

```bash
git add internal/cli/i18n_test.go
git commit -m "test(cli): add unit tests for i18n

- Test language setting
- Test message translation
- Test formatted messages
- Test language normalization"
```

---

## Final Verification

- [ ] **Run all tests**

```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/cli/... -v
```

- [ ] **Build and test CLI**

```bash
go build -o gateway.exe ./cmd/gateway
.\gateway.exe -lang zh validate 2>&1 || true
.\gateway.exe -lang en validate 2>&1 || true
```

---

**Plan Complete**
