# Backend i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement backend API error message internationalization supporting 7 languages (zh/en/ja/ko/es/fr/de) based on `admin.language` config.

**Architecture:** Create `internal/i18n/` package with Bundle type wrapping `golang.org/x/text/message`. Store translations in JSON files per language. Integrate with Admin API handlers to return translated error messages.

**Tech Stack:** Go, `golang.org/x/text/message`, JSON

---

## File Structure

| File | Purpose |
|------|---------|
| `internal/i18n/bundle.go` | Core i18n Bundle type with T() method |
| `internal/i18n/keys.go` | Error message key constants |
| `internal/i18n/loader.go` | JSON translation file loader |
| `internal/i18n/locales/en.json` | English translations |
| `internal/i18n/locales/zh.json` | Chinese translations |
| `internal/i18n/locales/ja.json` | Japanese translations |
| `internal/i18n/locales/ko.json` | Korean translations |
| `internal/i18n/locales/es.json` | Spanish translations |
| `internal/i18n/locales/fr.json` | French translations |
| `internal/i18n/locales/de.json` | German translations |
| `internal/i18n/bundle_test.go` | Unit tests for Bundle |
| `internal/adminapi/routes.go` | Modified to use i18n for error responses |
| `internal/core/errors.go` | Modified to use i18n keys |

---

## Task 1: Create i18n Package Structure

**Files:**
- Create: `internal/i18n/bundle.go`
- Create: `internal/i18n/keys.go`
- Create: `internal/i18n/loader.go`
- Create: `internal/i18n/locales/en.json`

---

- [ ] **Step 1.1: Create bundle.go with Bundle type**

Create `internal/i18n/bundle.go`:

```go
package i18n

import (
	"fmt"

	"golang.org/x/text/language"
	"golang.org/x/text/message"
)

// Bundle provides message translation for a specific language
type Bundle struct {
	lang    language.Tag
	printer *message.Printer
	catalog map[string]string // fallback for simple key-value
}

// New creates a new Bundle for the given language
func New(lang string) *Bundle {
	tag := language.MustParse(normalizeLang(lang))
	return &Bundle{
		lang:    tag,
		printer: message.NewPrinter(tag),
		catalog: make(map[string]string),
	}
}

// NewWithCatalog creates a Bundle with loaded translations
func NewWithCatalog(lang string, catalog map[string]string) *Bundle {
	b := New(lang)
	b.catalog = catalog
	return b
}

// T translates a message key with optional arguments
func (b *Bundle) T(key string, args ...interface{}) string {
	// First try catalog lookup
	if msg, ok := b.catalog[key]; ok {
		if len(args) > 0 {
			return fmt.Sprintf(msg, args...)
		}
		return msg
	}
	// Fall back to key itself
	return key
}

// SetLanguage changes the bundle language
func (b *Bundle) SetLanguage(lang string) {
	b.lang = language.MustParse(normalizeLang(lang))
	b.printer = message.NewPrinter(b.lang)
}

func normalizeLang(lang string) string {
	switch lang {
	case "zh", "zh-CN", "zh-TW":
		return "zh"
	case "en", "en-US", "en-GB":
		return "en"
	case "ja":
		return "ja"
	case "ko":
		return "ko"
	case "es":
		return "es"
	case "fr":
		return "fr"
	case "de":
		return "de"
	default:
		return "zh"
	}
}
```

---

- [ ] **Step 1.2: Create keys.go with error constants**

Create `internal/i18n/keys.go`:

```go
package i18n

// Error message keys for backend API
const (
	// Core errors
	ErrNoProvider      = "errors.no_provider"
	ErrModelNotFound   = "errors.model_not_found"
	ErrUpstreamTimeout = "errors.upstream_timeout"
	ErrRetryExhausted  = "errors.retry_exhausted"
	ErrRequestTooLarge = "errors.request_too_large"
	ErrUnauthorized    = "errors.unauthorized"
	ErrForbidden       = "errors.forbidden"

	// Auth errors
	ErrInvalidToken       = "errors.invalid_token"
	ErrInvalidRequestBody = "errors.invalid_request_body"
	ErrNoAuthCredential   = "errors.no_auth_credential"
	ErrMalformedCookie    = "errors.malformed_cookie"
	ErrSignatureMismatch  = "errors.signature_mismatch"
	ErrCookieExpired      = "errors.cookie_expired"

	// Config errors
	ErrConfigExportUnavailable     = "errors.config_export_unavailable"
	ErrConfigSaveUnavailable       = "errors.config_save_unavailable"
	ErrInvalidConfigPayload        = "errors.invalid_config_payload"
	ErrConfigHistoryUnavailable    = "errors.config_history_unavailable"
	ErrConfigDiffUnavailable       = "errors.config_diff_unavailable"
	ErrConfigRollbackUnavailable   = "errors.config_rollback_unavailable"
	ErrInvalidRollbackPayload      = "errors.invalid_rollback_payload"
	ErrInvalidUpstreamProbePayload = "errors.invalid_upstream_probe_payload"
)
```

---

- [ ] **Step 1.3: Create loader.go for JSON files**

Create `internal/i18n/loader.go`:

```go
package i18n

import (
	"embed"
	"encoding/json"
	"fmt"
)

//go:embed locales/*.json
var localeFS embed.FS

// LoadCatalog loads translation catalog for a language
func LoadCatalog(lang string) (map[string]string, error) {
	filename := fmt.Sprintf("locales/%s.json", normalizeLang(lang))
	data, err := localeFS.ReadFile(filename)
	if err != nil {
		return nil, fmt.Errorf("read locale file %s: %w", filename, err)
	}

	var catalog map[string]map[string]string
	if err := json.Unmarshal(data, &catalog); err != nil {
		return nil, fmt.Errorf("unmarshal locale %s: %w", lang, err)
	}

	// Flatten nested structure (e.g., {"errors": {"key": "value"}} -> {"errors.key": "value"})
	result := make(map[string]string)
	flattenMap("", catalog, result)
	return result, nil
}

func flattenMap(prefix string, src map[string]map[string]string, dst map[string]string) {
	for category, messages := range src {
		for key, value := range messages {
			fullKey := category + "." + key
			dst[fullKey] = value
		}
	}
}

// MustLoadCatalog loads catalog or panics on error (use in init)
func MustLoadCatalog(lang string) map[string]string {
	catalog, err := LoadCatalog(lang)
	if err != nil {
		// Fall back to empty catalog
		return make(map[string]string)
	}
	return catalog
}
```

---

- [ ] **Step 1.4: Create English locale file**

Create `internal/i18n/locales/en.json`:

```json
{
  "errors": {
    "no_provider": "no available provider for model",
    "model_not_found": "model not found",
    "upstream_timeout": "upstream request timed out",
    "retry_exhausted": "all retry attempts exhausted",
    "request_too_large": "request body too large",
    "unauthorized": "unauthorized",
    "forbidden": "forbidden",
    "invalid_token": "invalid token",
    "invalid_request_body": "invalid request body",
    "no_auth_credential": "no authentication credential",
    "malformed_cookie": "malformed cookie",
    "signature_mismatch": "signature mismatch",
    "cookie_expired": "cookie expired",
    "config_export_unavailable": "config export is not available",
    "config_save_unavailable": "config save is not available in this runtime",
    "invalid_config_payload": "invalid config payload",
    "config_history_unavailable": "config history is not available in this runtime",
    "config_diff_unavailable": "config history diff is not available in this runtime",
    "config_rollback_unavailable": "config rollback is not available in this runtime",
    "invalid_rollback_payload": "invalid rollback payload",
    "invalid_upstream_probe_payload": "invalid upstream probe payload"
  }
}
```

---

- [ ] **Step 1.5: Verify package compiles**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go build ./internal/i18n/...
```

Expected: No errors

---

- [ ] **Step 1.6: Commit**

```bash
git add internal/i18n/
git commit -m "feat(i18n): add backend i18n package structure

- Add Bundle type for message translation
- Add error key constants
- Add JSON loader with embed support
- Add English locale as base"
```

---

## Task 2: Create Unit Tests for Bundle

**Files:**
- Create: `internal/i18n/bundle_test.go`

---

- [ ] **Step 2.1: Write bundle tests**

Create `internal/i18n/bundle_test.go`:

```go
package i18n

import (
	"testing"
)

func TestNew(t *testing.T) {
	b := New("zh")
	if b == nil {
		t.Fatal("expected non-nil Bundle")
	}
}

func TestBundle_T(t *testing.T) {
	catalog := map[string]string{
		"errors.test": "test message",
		"errors.formatted": "hello %s",
	}
	b := NewWithCatalog("en", catalog)

	tests := []struct {
		key      string
		args     []interface{}
		expected string
	}{
		{"errors.test", nil, "test message"},
		{"errors.formatted", []interface{}{"world"}, "hello world"},
		{"errors.missing", nil, "errors.missing"}, // returns key if not found
	}

	for _, tt := range tests {
		t.Run(tt.key, func(t *testing.T) {
			result := b.T(tt.key, tt.args...)
			if result != tt.expected {
				t.Errorf("T(%q) = %q, want %q", tt.key, result, tt.expected)
			}
		})
	}
}

func TestNormalizeLang(t *testing.T) {
	tests := []struct {
		input    string
		expected string
	}{
		{"zh", "zh"},
		{"zh-CN", "zh"},
		{"en", "en"},
		{"en-US", "en"},
		{"ja", "ja"},
		{"ko", "ko"},
		{"es", "es"},
		{"fr", "fr"},
		{"de", "de"},
		{"unknown", "zh"}, // default
	}

	for _, tt := range tests {
		t.Run(tt.input, func(t *testing.T) {
			result := normalizeLang(tt.input)
			if result != tt.expected {
				t.Errorf("normalizeLang(%q) = %q, want %q", tt.input, result, tt.expected)
			}
		})
	}
}

func TestLoadCatalog(t *testing.T) {
	// This test requires the locale files to exist
	catalog, err := LoadCatalog("en")
	if err != nil {
		t.Fatalf("LoadCatalog failed: %v", err)
	}

	// Check some expected keys
	expectedKeys := []string{
		"errors.no_provider",
		"errors.unauthorized",
		"errors.invalid_token",
	}

	for _, key := range expectedKeys {
		if _, ok := catalog[key]; !ok {
			t.Errorf("expected key %q not found in catalog", key)
		}
	}
}
```

---

- [ ] **Step 2.2: Run tests**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/i18n/... -v
```

Expected: All tests pass

---

- [ ] **Step 2.3: Commit**

```bash
git add internal/i18n/bundle_test.go
git commit -m "test(i18n): add unit tests for Bundle

- Test T() method with and without formatting
- Test language normalization
- Test catalog loading"
```

---

## Task 3: Add Chinese Translations

**Files:**
- Create: `internal/i18n/locales/zh.json`

---

- [ ] **Step 3.1: Create Chinese locale file**

Create `internal/i18n/locales/zh.json`:

```json
{
  "errors": {
    "no_provider": "没有可用的模型提供商",
    "model_not_found": "模型未找到",
    "upstream_timeout": "上游请求超时",
    "retry_exhausted": "所有重试尝试已耗尽",
    "request_too_large": "请求体过大",
    "unauthorized": "未授权",
    "forbidden": "禁止访问",
    "invalid_token": "无效令牌",
    "invalid_request_body": "无效请求体",
    "no_auth_credential": "无认证凭据",
    "malformed_cookie": "Cookie格式错误",
    "signature_mismatch": "签名不匹配",
    "cookie_expired": "Cookie已过期",
    "config_export_unavailable": "配置导出不可用",
    "config_save_unavailable": "此运行时不支持配置保存",
    "invalid_config_payload": "无效配置数据",
    "config_history_unavailable": "此运行时不支持配置历史",
    "config_diff_unavailable": "此运行时不支持配置差异对比",
    "config_rollback_unavailable": "此运行时不支持配置回滚",
    "invalid_rollback_payload": "无效回滚数据",
    "invalid_upstream_probe_payload": "无效上游探测数据"
  }
}
```

---

- [ ] **Step 3.2: Add test for Chinese translation**

Add to `internal/i18n/bundle_test.go`:

```go
func TestBundle_T_Chinese(t *testing.T) {
	catalog := map[string]string{
		"errors.no_provider": "没有可用的模型提供商",
	}
	b := NewWithCatalog("zh", catalog)

	result := b.T("errors.no_provider")
	if result != "没有可用的模型提供商" {
		t.Errorf("Chinese translation failed: got %q", result)
	}
}
```

---

- [ ] **Step 3.3: Run tests**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/i18n/... -v -run TestBundle_T_Chinese
```

Expected: Test passes

---

- [ ] **Step 3.4: Commit**

```bash
git add internal/i18n/locales/zh.json internal/i18n/bundle_test.go
git commit -m "feat(i18n): add Chinese translations

- Add zh.json with complete error messages
- Add test for Chinese translation"
```

---

## Task 4: Add Remaining Language Translations

**Files:**
- Create: `internal/i18n/locales/ja.json`
- Create: `internal/i18n/locales/ko.json`
- Create: `internal/i18n/locales/es.json`
- Create: `internal/i18n/locales/fr.json`
- Create: `internal/i18n/locales/de.json`

---

- [ ] **Step 4.1: Create Japanese locale file**

Create `internal/i18n/locales/ja.json`:

```json
{
  "errors": {
    "no_provider": "モデルプロバイダーが利用できません",
    "model_not_found": "モデルが見つかりません",
    "upstream_timeout": "アップストリームリクエストがタイムアウトしました",
    "retry_exhausted": "すべての再試行が失敗しました",
    "request_too_large": "リクエストボディが大きすぎます",
    "unauthorized": "認証されていません",
    "forbidden": "アクセスが拒否されました",
    "invalid_token": "無効なトークンです",
    "invalid_request_body": "無効なリクエストボディです",
    "no_auth_credential": "認証情報がありません",
    "malformed_cookie": "Cookieの形式が正しくありません",
    "signature_mismatch": "署名が一致しません",
    "cookie_expired": "Cookieの有効期限が切れました",
    "config_export_unavailable": "設定のエクスポートは利用できません",
    "config_save_unavailable": "このランタイムでは設定の保存は利用できません",
    "invalid_config_payload": "無効な設定データです",
    "config_history_unavailable": "このランタイムでは設定履歴は利用できません",
    "config_diff_unavailable": "このランタイムでは設定差分は利用できません",
    "config_rollback_unavailable": "このランタイムでは設定ロールバックは利用できません",
    "invalid_rollback_payload": "無効なロールバックデータです",
    "invalid_upstream_probe_payload": "無効なアップストリームプローブデータです"
  }
}
```

---

- [ ] **Step 4.2: Create Korean locale file**

Create `internal/i18n/locales/ko.json`:

```json
{
  "errors": {
    "no_provider": "사용 가능한 모델 제공자가 없습니다",
    "model_not_found": "모델을 찾을 수 없습니다",
    "upstream_timeout": "업스트림 요청 시간 초과",
    "retry_exhausted": "모든 재시도 시도가 소진되었습니다",
    "request_too_large": "요청 본문이 너무 큽니다",
    "unauthorized": "인증되지 않음",
    "forbidden": "접근 금지",
    "invalid_token": "유효하지 않은 토큰",
    "invalid_request_body": "유효하지 않은 요청 본문",
    "no_auth_credential": "인증 자격 증명 없음",
    "malformed_cookie": "잘못된 형식의 쿠키",
    "signature_mismatch": "서명 불일치",
    "cookie_expired": "쿠키 만료됨",
    "config_export_unavailable": "구성 낼 수 없음",
    "config_save_unavailable": "이 런타임에서 구성 저장을 사용할 수 없음",
    "invalid_config_payload": "유효하지 않은 구성 데이터",
    "config_history_unavailable": "이 런타임에서 구성 기록을 사용할 수 없음",
    "config_diff_unavailable": "이 런타임에서 구성 차이를 사용할 수 없음",
    "config_rollback_unavailable": "이 런타임에서 구성 롤백을 사용할 수 없음",
    "invalid_rollback_payload": "유효하지 않은 롤백 데이터",
    "invalid_upstream_probe_payload": "유효하지 않은 업스트림 프로브 데이터"
  }
}
```

---

- [ ] **Step 4.3: Create Spanish locale file**

Create `internal/i18n/locales/es.json`:

```json
{
  "errors": {
    "no_provider": "no hay proveedor disponible para el modelo",
    "model_not_found": "modelo no encontrado",
    "upstream_timeout": "tiempo de espera de solicitud agotado",
    "retry_exhausted": "todos los intentos de reintento agotados",
    "request_too_large": "cuerpo de solicitud demasiado grande",
    "unauthorized": "no autorizado",
    "forbidden": "prohibido",
    "invalid_token": "token inválido",
    "invalid_request_body": "cuerpo de solicitud inválido",
    "no_auth_credential": "sin credencial de autenticación",
    "malformed_cookie": "cookie malformada",
    "signature_mismatch": "firma no coincide",
    "cookie_expired": "cookie expirada",
    "config_export_unavailable": "exportación de configuración no disponible",
    "config_save_unavailable": "guardado de configuración no disponible en este runtime",
    "invalid_config_payload": "carga de configuración inválida",
    "config_history_unavailable": "historial de configuración no disponible en este runtime",
    "config_diff_unavailable": "diff de historial no disponible en este runtime",
    "config_rollback_unavailable": "rollback de configuración no disponible en este runtime",
    "invalid_rollback_payload": "carga de rollback inválida",
    "invalid_upstream_probe_payload": "carga de prueba de upstream inválida"
  }
}
```

---

- [ ] **Step 4.4: Create French locale file**

Create `internal/i18n/locales/fr.json`:

```json
{
  "errors": {
    "no_provider": "aucun fournisseur disponible pour le modèle",
    "model_not_found": "modèle non trouvé",
    "upstream_timeout": "délai de requête en amont dépassé",
    "retry_exhausted": "toutes les tentatives de réessai épuisées",
    "request_too_large": "corps de requête trop grand",
    "unauthorized": "non autorisé",
    "forbidden": "interdit",
    "invalid_token": "jeton invalide",
    "invalid_request_body": "corps de requête invalide",
    "no_auth_credential": "aucune information d'identification",
    "malformed_cookie": "cookie malformé",
    "signature_mismatch": "signature ne correspond pas",
    "cookie_expired": "cookie expiré",
    "config_export_unavailable": "exportation de configuration non disponible",
    "config_save_unavailable": "enregistrement de configuration non disponible dans ce runtime",
    "invalid_config_payload": "charge de configuration invalide",
    "config_history_unavailable": "historique de configuration non disponible dans ce runtime",
    "config_diff_unavailable": "différence d'historique non disponible dans ce runtime",
    "config_rollback_unavailable": "restauration de configuration non disponible dans ce runtime",
    "invalid_rollback_payload": "charge de restauration invalide",
    "invalid_upstream_probe_payload": "charge de test d'amont invalide"
  }
}
```

---

- [ ] **Step 4.5: Create German locale file**

Create `internal/i18n/locales/de.json`:

```json
{
  "errors": {
    "no_provider": "kein Anbieter für Modell verfügbar",
    "model_not_found": "Modell nicht gefunden",
    "upstream_timeout": "Upstream-Anfrage Zeitüberschreitung",
    "retry_exhausted": "alle Wiederholungsversuche erschöpft",
    "request_too_large": "Anfragekörper zu groß",
    "unauthorized": "nicht autorisiert",
    "forbidden": "verboten",
    "invalid_token": "ungültiges Token",
    "invalid_request_body": "ungültiger Anfragekörper",
    "no_auth_credential": "keine Anmeldeinformationen",
    "malformed_cookie": "fehlerhaftes Cookie",
    "signature_mismatch": "Signatur stimmt nicht überein",
    "cookie_expired": "Cookie abgelaufen",
    "config_export_unavailable": "Konfigurationsexport nicht verfügbar",
    "config_save_unavailable": "Konfigurationsspeicherung in dieser Laufzeit nicht verfügbar",
    "invalid_config_payload": "ungültige Konfigurationsnutzlast",
    "config_history_unavailable": "Konfigurationsverlauf in dieser Laufzeit nicht verfügbar",
    "config_diff_unavailable": "Konfigurationsverlaufsdiff in dieser Laufzeit nicht verfügbar",
    "config_rollback_unavailable": "Konfigurationsrollback in dieser Laufzeit nicht verfügbar",
    "invalid_rollback_payload": "ungültige Rollback-Nutzlast",
    "invalid_upstream_probe_payload": "ungültige Upstream-Testnutzlast"
  }
}
```

---

- [ ] **Step 4.6: Run all locale tests**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/i18n/... -v
```

Expected: All tests pass

---

- [ ] **Step 4.7: Commit**

```bash
git add internal/i18n/locales/ja.json internal/i18n/locales/ko.json \
  internal/i18n/locales/es.json internal/i18n/locales/fr.json internal/i18n/locales/de.json
git commit -m "feat(i18n): add remaining language translations

- Add Japanese (ja), Korean (ko), Spanish (es)
- Add French (fr), German (de) translations
- All 7 languages now supported"
```

---

## Task 5: Integrate i18n with Admin API

**Files:**
- Modify: `internal/adminapi/routes.go`
- Modify: `internal/adminapi/server.go` (if exists, or create)

---

- [ ] **Step 5.1: Read current adminapi structure**

Run:
```bash
head -100 /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway/internal/adminapi/routes.go
```

Note the Server struct or handler pattern used.

---

- [ ] **Step 5.2: Add i18n Bundle to Server struct**

If Server struct exists in routes.go, add i18n field:

```go
import "ai-model-gateway/internal/i18n"

type Server struct {
    // ... existing fields
    i18n *i18n.Bundle
}

// NewServer creates server with i18n
func NewServer(cfg *core.Config, store TelemetryStore, ...) *Server {
    // ... existing code
    
    // Load i18n catalog
    catalog := i18n.MustLoadCatalog(cfg.Admin.Language)
    i18nBundle := i18n.NewWithCatalog(cfg.Admin.Language, catalog)
    
    return &Server{
        // ... existing fields
        i18n: i18nBundle,
    }
}
```

---

- [ ] **Step 5.3: Update error responses to use i18n**

Find all error responses in routes.go like:
```go
writeJSON(w, http.StatusUnauthorized, map[string]string{"error": "unauthorized"})
```

Replace with:
```go
writeJSON(w, http.StatusUnauthorized, map[string]string{"error": s.i18n.T(i18n.ErrUnauthorized)})
```

Do this for all error responses:
- `invalid token` -> `i18n.ErrInvalidToken`
- `invalid request body` -> `i18n.ErrInvalidRequestBody`
- `config export is not available` -> `i18n.ErrConfigExportUnavailable`
- etc.

---

- [ ] **Step 5.4: Test admin API with curl**

Start server and test:
```bash
# Test with zh language config
curl -X POST http://localhost:18080/api/admin/v2/auth/login \
  -H "Content-Type: application/json" \
  -d '{"token": "invalid"}'
```

Expected: Response contains Chinese error message "无效令牌"

---

- [ ] **Step 5.5: Commit**

```bash
git add internal/adminapi/routes.go
git commit -m "feat(adminapi): integrate i18n for error messages

- Add i18n Bundle to Server struct
- Update all error responses to use translated messages
- Support 7 languages based on admin.language config"
```

---

## Task 6: Add go.mod Dependency

**Files:**
- Modify: `go.mod`

---

- [ ] **Step 6.1: Add golang.org/x/text dependency**

Run:
```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go get golang.org/x/text/message golang.org/x/text/language
go mod tidy
```

---

- [ ] **Step 6.2: Verify build**

Run:
```bash
go build ./...
```

Expected: No errors

---

- [ ] **Step 6.3: Commit**

```bash
git add go.mod go.sum
git commit -m "deps: add golang.org/x/text for i18n support

- Add message and language packages
- Run go mod tidy"
```

---

## Final Verification

- [ ] **Run all tests**

```bash
cd /C/Users/96152/My-Project/Active/Software/AI-Model-Gateway
go test ./internal/i18n/... ./internal/adminapi/... -v
```

Expected: All tests pass

- [ ] **Build entire project**

```bash
go build ./...
```

Expected: Success

---

**Plan Complete**
